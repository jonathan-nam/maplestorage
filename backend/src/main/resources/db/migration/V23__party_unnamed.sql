-- A config does not get a name.
--
-- It is one of your characters, on one boss, with the people in it: that is what it IS, and every
-- one of those is already on screen. A label was one more box to fill in that said nothing the row
-- did not, and an empty one read as something missing.
--
-- Dropped rather than left unused. A column nothing writes is a column the next reader has to work
-- out the status of.

ALTER TABLE party DROP COLUMN name;
