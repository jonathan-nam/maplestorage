# Scratch

Drop zone for screenshots handed over to work a feature or a bug. Everything in
here is gitignored except this file, so anything dropped here is disposable and
can be deleted at any time.

Nothing here is a fixture. A capture becomes one by moving up into
`test-fixtures/` and gaining a test that reads it, in the same commit.
