// The only prose on the site, and it earns its place: the app serves Nexon's art (boss portraits
// cut from a capture, item icons, the count digits), so it has to say whose it is and that nobody
// endorsed this. Two sentences is the whole budget.
export function SiteFooter() {
  return (
    <footer className="site-footer">
      Not affiliated with or endorsed by Nexon. MapleStory and all game assets are the property of
      Nexon.
    </footer>
  );
}
