// How many sprites a row of them shows at once, everywhere in the app. The strip enforces it as a
// width (.carousel-track in globals.css); this is the same number for the placeholders that stand
// in for a strip while it loads. carousel.test.ts holds the two together.
export const TILES_SHOWN = 4;
